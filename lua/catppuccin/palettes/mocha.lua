-- ~/lua/catppuccin/palettes/mocha.lua
return {
    -- Акцентные цвета (из Quill syntax)
    rosewater = "#f8f8f2",   -- near-white (embedded, labels)
    flamingo  = "#f28c61",   -- orange-coral (identifiers)
    pink      = "#f0abfc",   -- fuchsia (strings, attributes)
    mauve     = "#bd93f9",   -- purple (enums, specials)
    red       = "#ff5555",   -- red (regex, errors)
    maroon    = "#f28c61",   -- orange (error variants)
    peach     = "#f28c61",   -- orange (functions, constants)
    yellow    = "#fdba74",   -- warm yellow (properties, warnings)
    green     = "#cff486",   -- lime green (tags, success)
    teal      = "#5eead4",   -- teal (cursor, accents)
    sky       = "#8be9fd",   -- bright cyan (type.super, links)
    sapphire  = "#6272a4",   -- blue-gray (preproc)
    blue      = "#a78bfa",   -- violet (keywords, numbers)
    lavender  = "#a78bfa",   -- violet (UI accents)

    -- Текстовая шкала (Tailwind Stone ↑)
    text      = "#e7e5e4",   -- stone-200 — основной текст
    subtext1  = "#d6d3d1",   -- stone-300 — editor foreground
    subtext0  = "#a8a29e",   -- stone-400 — приглушённый текст
    overlay2  = "#a8a29e",   -- stone-400 — комментарии
    overlay1  = "#78716c",   -- stone-500 — placeholder, faint
    overlay0  = "#57534e",   -- stone-600 — номера строк

    -- Фоновая шкала (Tailwind Stone ↓)
    surface2  = "#4e4845",   -- stone-650 — subtle highlight
    surface1  = "#44403c",   -- stone-700 — бордеры, selection
    surface0  = "#363130",   -- stone-750 — panel background
    base      = "#292524",   -- stone-800 — основной фон
    mantle    = "#1c1917",   -- stone-900 — editor background
    crust     = "#0c0a09",   -- stone-950 — title bar (самый тёмный)
}
